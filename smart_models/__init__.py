"""Smart Document models for the knowledge tool.

This directory is a pluggable models directory registered in `knowledge_config.yaml`.
Each `.py` file (not prefixed with underscore) contains one or more
RenderableModel subclasses discovered by the knowledge tool at runtime.
"""
