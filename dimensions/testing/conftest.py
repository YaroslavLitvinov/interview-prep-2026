"""pytest hooks for the testing module.

Reserved for future fixtures / plugins. Scenario auto-discovery now
lives in `dimensions/tests/test_scenario_replay.py` so it's picked up
by any `pytest <subtree>` invocation rather than requiring the conftest
to be targeted directly.
"""
